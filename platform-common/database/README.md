# platform-common 用データベーススクリプト

- **migrations/** — 共有 DB（開発: `alrfy_ai_db_dev` / 本番: `alrfy_ai_db_pro`）に対する DDL と必須シード。
- **seeds/** — 開発専用のデータ投入（本番では実行しない）。

詳細は [docs/acl_database.md](../docs/acl_database.md) を参照。
